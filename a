 k get pods --no-headers=true -o custom-columns=ns:.metadata.namespace,name:.metadata.name,status:.status.phase | awk '{printf "kubectl -n %s delete pod %s --force --grace-period=0 \n", $1, $2}' | bash
